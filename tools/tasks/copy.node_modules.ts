import {join} from 'path';
import {NODE_MODULES_SRC, APP_DEST} from '../config';

export = function copyNodeModules(gulp, plugins) {
  return function () {
    return gulp.src([join(NODE_MODULES_SRC, '**/*')])
      .pipe(gulp.dest(join(APP_DEST, NODE_MODULES_SRC)));
  };
};
